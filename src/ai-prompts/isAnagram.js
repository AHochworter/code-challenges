/** FROM DEVIN
 Write a function isAnagram(str1, str2) that takes in two strings as arguments and returns true if the strings are anagrams of each other, and false otherwise. Anagrams are words or phrases that contain the same characters, but in a different order. 
 */

const isAnagram = () => {
  console.log('Is it an Anagram');
};

export default isAnagram;
