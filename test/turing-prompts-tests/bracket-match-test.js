import doBracketsMatch from '../../src/turing-prompts/bracket-match.js';
import { expect } from 'chai';

describe('doBracketsMatch', () => {
  it('should be a function', () => {
    expect(doBracketsMatch).to.be.a('function');
  });
});
