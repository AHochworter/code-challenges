import findMaxAndMin from '../../src/ai-prompts/findMaxAndMin.js';
import { expect } from 'chai';

describe('findMaxAndMin', () => {
  it('should be a function', () => {
    expect(findMaxAndMin).to.be.a('function');
  });
});
