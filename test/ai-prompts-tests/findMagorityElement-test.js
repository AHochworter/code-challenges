import findMajorityElement from '../../src/ai-prompts/findMajorityElement.js';
import { expect } from 'chai';

describe('findMajorityElement', () => {
  it('should be a function', () => {
    expect(findMajorityElement).to.be.a('function');
  });
});
